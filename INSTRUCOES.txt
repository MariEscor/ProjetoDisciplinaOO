No arquivo projeto/jogo.py faça os seguintes ajustes:

1️⃣ No topo:
import pygame
from bag_manager import add_pokemon_to_bag
from bag_window import draw_bag_window

2️⃣ Na inicialização do loop principal:
show_bag = False

3️⃣ Nos eventos:
for event in pygame.event.get():
    if event.type == pygame.KEYDOWN:
        if event.key == pygame.K_b:
            show_bag = not show_bag
        if event.key == pygame.K_a:
            add_pokemon_to_bag("pikachu")  # Troque por variável real do Pokémon escolhido

4️⃣ No desenho da tela (logo após screen.fill ou similar):
if show_bag:
    draw_bag_window(screen)